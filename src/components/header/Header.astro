---
import logo from './kiara-toru.png';

---
<style>
  .root {
    height: 60px;
    width: 100%;
    background-color: var(--white);
    box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25), 0px 30px 60px -30px rgba(0, 0, 0, 0.3);

    display: flex;
    justify-content: center;
  }

  .header {
    width: 100%;
    max-width: var(--max-width);

    display: flex;
    justify-content: space-between;
  }

  .logo {
    object-fit: cover;
    max-width: 100%;
    max-height: 100%;
  }

  .controls {
    display: flex;
    align-items: center;
    column-gap: 48px;
  }

  .controls > a {
    text-decoration: none;
    font-weight: 700;
    color: var(--black);
    font-size: 12px;
    line-height: 14px;
  }
  .controls > a:hover {
    text-decoration: none;
    color: var(--kiara-orange);
  }

  .mobile-controls {
    visibility: hidden;
  }
</style>

<header class="root">
  <div class="header">
    <a href="#" >
      <img src={logo} class="logo" />
    </a>
    <div class="controls">
      <a href="#contact-con">How to contact your local convention?</a>
      <a href="#contact-cover">How to get holoEN at your convention?</a>
      <a href="#about-us">About us</a>
    </div> 
    <div class="mobile-controls">mobile</div>
  </div>
</header>